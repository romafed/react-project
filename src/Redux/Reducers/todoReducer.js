import { } from '../types';

const initialState = {};

function todoReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default todoReducer;